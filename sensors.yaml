#Morning Commute Google Travel Time Sensor
  - platform: google_travel_time
    name: Morning Commute
    api_key: !secret gmaps
    origin: !secret haddr
    destination: !secret oaddr
    options:
      mode: driving
#Evening Commute Google Travel Time Sensor
  - platform: google_travel_time
    name: Evening Commute
    api_key: !secret gmaps
    origin: !secret oaddr
    destination: !secret haddr
    options:
      mode: driving

  - platform: authenticated

  - platform: template
    sensors:
      bedroom_motion:
        friendly_name: Bedroom
        value_template: "{%if states.sensor.bedroom_multisensor_burglar.state == '8' %}Detected{% elif states.sensor.bedroom_multisensor_burglar.state == '3' %}Tamper{% elif states.sensor.bedroom_multisensor_burglar.state == '0' %}Clear{% endif %}"
      # jeep_fuel:
      #   unit_of_measurement: '%'
      #   value_template: "{{ state_attr('device_tracker.2017_jeep_grand_cherokee', 'fuel_level') }}"      
      # jeep_location:
      #   value_template: "{{ state_attr('device_tracker.2017_jeep_grand_cherokee', 'latitude'), state_attr('device_tracker.2017_jeep_grand_cherokee', 'longitude') }}"

  # - platform: travisci
  #   api_key: !secret travis
  #   repository: 
  #     - home-assistant
  #   monitored_conditions:
  #     - last_build_id
  #     - last_build_state

  # - platform: rest
  #   resource: https://api.travis-ci.com/repos/schettada/home-assistant/builds
  #   headers:
  #     Accept: application/atom+xml
  #   value_template: "{{ value_json.builds[0].state }}"
  #   name: "Travis Build Status"

  # - platform: scrape
  #   resource: https://api.travis-ci.com/repos/schettada/home-assistant/builds
  #   headers:
  #     Accept: application/atom+xml
  #   select: "State"

#GeoCode Sensor
#  - platform: google_geocode
#    origin: device_tracker.sooraj
#    name: Sooraj
#    options: street_number, street, city
#    api_key: !secret maps
#System Monitor
#  - platform: systemmonitor
#    resources:
#      - type: disk_use_percent
#        arg: /home
#      - type: memory_use_percent
#      - type: processor_use
#      - type: load_15m
#      - type: ipv4_address